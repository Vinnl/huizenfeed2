pages:
  stage: deploy
  image: mcr.microsoft.com/playwright:v1.15.1-focal
  script:
  - DEBUG=info npx feed-me-up-scotty@main
  artifacts:
    paths:
    - public
    expire_in: 1 day
  only:
  - main
  retry:
    max: 2
    when: always
